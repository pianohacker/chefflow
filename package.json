{
  "scripts": {
    "test": "mocha --reporter mocha-better-spec-reporter"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "mocha-better-spec-reporter": "^3.1.0"
  },
  "dependencies": {
    "debug": "^4.1.0"
  }
}
